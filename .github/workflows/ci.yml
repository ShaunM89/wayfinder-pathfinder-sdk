name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint with ruff
        run: ruff check src tests

      - name: Format check with black
        run: black --check src tests

      - name: Type check with mypy
        run: mypy src/pathfinder_sdk

      - name: Test with pytest
        run: pytest --tb=short -q

      - name: Validate examples are syntactically valid
        run: |
          python -m py_compile examples/01_basic_ranking.py
          python -m py_compile examples/02_with_llm_agent.py
          python -m py_compile examples/03_batch_processing.py
          python -m py_compile examples/04_custom_filter.py

  build:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Build wheel
        run: python -m build --wheel

      - name: Check wheel size
        run: |
          WHEEL_SIZE=$(stat -c%s dist/*.whl)
          MAX_SIZE=262144000  # 250 MB
          echo "Wheel size: $WHEEL_SIZE bytes"
          if [ "$WHEEL_SIZE" -gt "$MAX_SIZE" ]; then
            echo "Wheel exceeds 250MB limit"
            exit 1
          fi
